// Live Chat inbox landing — shown at /chat/inbox before a conversation is
// selected.

export default function ChatInboxIndexPage(): React.JSX.Element {
  return (
    <div className="text-base-content flex h-full items-center justify-center p-8 text-center text-sm">
      Select a conversation from the list to start replying.
    </div>
  );
}
