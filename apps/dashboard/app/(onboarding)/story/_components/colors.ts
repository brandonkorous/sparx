// The module-tint recipe lives in `@sparx/story-schemas` so the composer's editable
// chips and the marketing hero's read-only ones share one definition.
export { moduleVar, tintStyle, dotStyle } from '@sparx/story-schemas';
