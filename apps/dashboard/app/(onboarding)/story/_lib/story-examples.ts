// The example stories live in `@sparx/story-schemas` — the composer's starting-point
// templates AND the marketing hero's rotating showcase read the same list.
export { STORY_EXAMPLES } from '@sparx/story-schemas';
export type { StoryExample } from '@sparx/story-schemas';
