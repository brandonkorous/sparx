# [Reference brand] — design study → `sparx-<slug>`

**Version:** 1.0.0
**Author:** Brandon Korous
**Last Updated:** 2026-08-04
**Reference:** <brand + live URL studied>
**Archetype:** <one-line design language>
**sparx slug:** `<slug>` · **Example vertical:** <ours> · **Theme:** <preset name or "bespoke — see §Theme">

> Faithfulness bar: **closest clone allowed** — mimic structure AND aesthetic feel,
> sparx components + branding only, no trademarked assets. See [README](../README.md).

## 1. Why this reference

<2–4 sentences: what the brand is, why its storefront is a design exemplar, what
design language it anchors in our set of ten.>

## 2. Screenshots

Captured to `./images/`. List each with a one-line caption.

- `home-fold.png` — <caption>
- `home-full.png` — <full homepage scroll>
- `nav.png` — <header / mega-nav / mobile menu>
- `pdp.png` — <product detail page>
- `plp.png` — <collection / listing>
- `footer.png` — <footer>
- <any signature moment: lookbook, subscription flow, mission band, etc.>

## 3. Design language

- **Palette:** <grounds, primary, accents; the _mood_, in words + hex observed>
- **Typography:** <display + body pairing, weights, case, scale feel; serif/sans>
- **Imagery:** <photography style, crop, aspect, treatment; illustration?>
- **Shape & density:** <radius, borders, whitespace, grid columns, section padding>
- **Motion:** <scroll reveals, hover, video, carousels — if signature>

## 4. Layout anatomy (top to bottom)

- **Announcement / utility bar:** <…>
- **Header / nav:** <zones, alignment, mega-nav, sticky behaviour, cart>
- **Hero:** <structure: full-bleed image? split? video? headline weight, CTA count>
- **Homepage section sequence:** numbered list of every band in order, with the
  job each does (category tiles, featured grid, editorial, social proof, mission,
  newsletter, …). This IS the blueprint's home composition.
- **PDP anatomy:** <gallery layout, buy-box position, variant UI, upsell, reviews>
- **Collection / PLP:** <grid density, filters, card anatomy>
- **Footer:** <columns, newsletter, legal, socials>

## 5. Signature interaction patterns

<The 2–4 things that make this site feel like itself and that we should reproduce.>

## 6. The sparx translation

- **Theme:** <exact preset from the 20, OR bespoke palette — if bespoke, give the
  full token direction: grounds (4 surfaces), primary strategy, accents, fonts.
  Must clear AA per the catalog-sweep.>
- **Section mapping:** table — each homepage band → the `SPARX_CATALOG` section key
  that realises it (or "NEW: <name>" if the catalog lacks it → flag as a catalog add).
- **Example business:** <name, one-liner, product/content set: what commerce +
  cms.blog_post records the bundle seeds so the bound grids render.>
- **Design freedom used:** <which tenant-only affordances — shadow/glass/gradient/
  full-bleed video — the reference needs that sparx surfaces would forbid.>
- **Deliberate departures:** <where we diverge from the reference and why.>

## 7. Build notes / catalog gaps

<Anything the build needs that doesn't exist yet: a missing section pattern, a
hero variant, a PDP arrangement. These become catalog additions (propagate),
never per-bundle inlines.>
