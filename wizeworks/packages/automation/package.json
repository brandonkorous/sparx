{
  "name": "@wizeworks/automation",
  "version": "0.0.0",
  "private": true,
  "description": "sparx Automation engine — the cross-module rule engine: entity-resolver registry, condition evaluator, gated dispatcher (the only path to an effect), action executors, durable resumable run state machine, and the automation service layer (docs/81-automation-module.md, docs/84-automation-build-log.md).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0",
    "@wizeworks/automation-schemas": "workspace:*",
    "@wizeworks/db": "workspace:*",
    "@wizeworks/events": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
