{
  "name": "@wizeworks/staff",
  "version": "0.0.0",
  "private": true,
  "description": "sparx Staff (docs/149) — the people who do the work: the roster, effective-dated pay rates, timesheets, shifts, time off, certifications, and the labour deriver that turns approved hours into a finance expense. Deliberately NOT payroll (no withholding, no filing, no payments) — sparx records hours and rates and exports them.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./schemas": {
      "types": "./src/schemas.ts",
      "default": "./src/schemas.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@wizeworks/db": "workspace:*",
    "@wizeworks/finance": "workspace:*",
    "@wizeworks/modules": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
