{
  "name": "@wizeworks/payments",
  "version": "0.0.0",
  "private": true,
  "description": "Vendor-agnostic payment gateway abstraction (docs/94 ADR). Every payment flow — storefront checkout, invoice payment links, B2B order payments — goes through PaymentService and never knows which gateway is behind it. sparx Pay = Stripe Connect destination charges (flat 0.5% via application_fee); Stripe Direct = the merchant's own account (no platform fee).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@wizeworks/db": "workspace:*",
    "@wizeworks/integrations": "workspace:*",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
