{
  "name": "@wizeworks/time",
  "version": "0.0.0",
  "private": true,
  "description": "sparx time — timezone-correct wall-clock math on the platform Intl tz database. Zero dependencies, no I/O, no Date.now(): every function takes explicit instants so callers stay deterministic and testable. Shared by scheduling availability (docs/79) and CRM service-level agreements (docs/144 §7.3).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
