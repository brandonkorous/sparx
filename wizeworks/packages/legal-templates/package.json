{
  "name": "@wizeworks/legal-templates",
  "version": "0.0.0",
  "private": true,
  "description": "sparx legal-page starter templates (docs/42 §3) — the platform-authored privacy/terms/cookie/returns/shipping/refund pages seeded into a new tenant's CMS as editable drafts. Dependency-free (no @wizeworks/db, no React) so the seed worker and api-rest can both import it.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
