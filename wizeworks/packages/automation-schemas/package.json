{
  "name": "@wizeworks/automation-schemas",
  "version": "0.0.0",
  "private": true,
  "description": "sparx Automation — Zod schemas + types for the automation rule document (trigger / condition / action / gate-log) shared across engine, REST, MCP (docs/81-automation-module.md, docs/84-automation-build-log.md).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
