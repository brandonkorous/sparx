{
  "name": "@wizeworks/voice",
  "version": "0.0.0",
  "private": true,
  "description": "sparx voice — a provider-agnostic calling abstraction (Twilio-first, swappable). Mirrors @wizeworks/sms: every call goes through one interface and never knows the vendor. Credentials are the TENANT's own, never the platform's (docs/144 §5.6).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
