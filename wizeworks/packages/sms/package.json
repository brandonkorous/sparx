{
  "name": "@wizeworks/sms",
  "version": "0.0.0",
  "private": true,
  "description": "sparx SMS — a provider-agnostic text-messaging abstraction (Twilio-first, swappable). Mirrors @wizeworks/payments: every caller sends through one interface and never knows the vendor. Used by the scheduling-worker for booking reminders/confirmations (docs/79 §10).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
