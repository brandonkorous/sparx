{
  "name": "@wizeworks/registrar",
  "version": "0.0.0",
  "private": true,
  "description": "Provider-agnostic domain-registrar contract — the RegistrarClient interface, shared domain types, RegistrarError, and the registrar-neutral DNS/DKIM helpers. Implemented by @wizeworks/godaddy (and future @wizeworks/namecom); consumed by api-rest and api-mcp through a composition-root resolver.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
