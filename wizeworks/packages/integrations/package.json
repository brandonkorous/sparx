{
  "name": "@wizeworks/integrations",
  "version": "0.0.0",
  "private": true,
  "description": "sparx Integrations — the one registry every outside service registers into. Categories, the shared descriptor, and typed per-kind facades so each domain keeps its own contract.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./types": {
      "types": "./src/types.ts",
      "default": "./src/types.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@wizeworks/modules": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^4.1.7"
  }
}
