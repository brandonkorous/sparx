{
  "name": "@wizeworks/events",
  "version": "0.0.0",
  "private": true,
  "description": "Shared event type registry + a transport-agnostic publisher. NATS JetStream (portable, in-cluster) and Google Pub/Sub are adapters behind one interface; the transport is chosen by EVENT_BROKER, never by a cloud provider's env var.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^5.3.0",
    "nats": "^2.29.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
