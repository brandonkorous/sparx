node_modules
dist
build
.next
.turbo
coverage
pnpm-lock.yaml
*.tsbuildinfo

# Terraform-managed directories
**/.terraform/**

# Prisma-generated SQL migrations
packages/db/prisma/migrations/**

# Auto-generated Next.js declaration
**/next-env.d.ts

# Playwright auth state (regenerated per test run)
**/playwright/.auth/**

# Locally-installed Claude Code skills (untracked, regenerated on install)
.claude/skills/**

# Impeccable local hook consent + cache (regenerated on hook run; git-ignored).
# The design spec (apps/*/.impeccable/design.json) IS tracked and stays formatted —
# only the *.local.json / *.cache.json scratch + the live/ runtime dir are skipped.
**/.impeccable/*.local.json
**/.impeccable/*.cache.json
**/.impeccable/live/**

# Agent git worktrees — ephemeral isolated checkouts, formatted on their own branch
.claude/worktrees/**

# Marketplace bundle-authoring scratch
marketplace-catalog/_src/**
marketplace-catalog/_gen/**
marketplace-catalog/_assets/**
apps/dashboard/_marketplace-assets.mjs

# Local media storage (LocalStorage MEDIA_LOCAL_DIR — generated artifacts)
**/.media-tmp/**

# Brain — the Obsidian knowledge vault (docs/brain). Free-form notes + Obsidian's
# own .obsidian/*.json editor state; excluded from every push gate (Prettier here,
# ESLint ignores mirror this). Authored for humans, never machine-formatted.
docs/brain/**
