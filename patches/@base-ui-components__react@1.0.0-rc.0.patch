diff --git a/esm/toast/content/ToastContent.js b/esm/toast/content/ToastContent.js
index 606fbf8414cfe373a66a5df64a65df2dec30b882..802fda1bed7c89a2a712b814c4580439995f3ab2 100644
--- a/esm/toast/content/ToastContent.js
+++ b/esm/toast/content/ToastContent.js
@@ -2,7 +2,7 @@
 
 import * as React from 'react';
 import { useIsoLayoutEffect } from '@base-ui-components/utils/useIsoLayoutEffect';
-import { useToastRootContext } from "../root/ToastRootContext.js";
+import { runInCommitPhase, useToastRootContext } from "../root/ToastRootContext.js";
 import { useRenderElement } from "../../utils/useRenderElement.js";
 
 /**
@@ -28,7 +28,10 @@ export const ToastContent = /*#__PURE__*/React.forwardRef(function ToastContent(
     if (!node) {
       return undefined;
     }
-    recalculateHeight();
+    // SPARX PATCH: this call is inside a layout effect, so mark it — see
+    // ToastRootContext.js. The two observers below are NOT: they fire
+    // asynchronously after paint and keep their synchronous flush.
+    runInCommitPhase(recalculateHeight);
     if (typeof ResizeObserver !== 'function' || typeof MutationObserver !== 'function') {
       return undefined;
     }
diff --git a/esm/toast/root/ToastRoot.js b/esm/toast/root/ToastRoot.js
index 7a4b511950f49aa394687c7e381e1ea6f9ae8699..f10c7002f3f4121abebd68be0c1ef0952926dcd4 100644
--- a/esm/toast/root/ToastRoot.js
+++ b/esm/toast/root/ToastRoot.js
@@ -7,7 +7,7 @@ import { inertValue } from '@base-ui-components/utils/inertValue';
 import { useIsoLayoutEffect } from '@base-ui-components/utils/useIsoLayoutEffect';
 import { useStableCallback } from '@base-ui-components/utils/useStableCallback';
 import { activeElement, contains, getTarget } from "../../floating-ui-react/utils.js";
-import { ToastRootContext } from "./ToastRootContext.js";
+import { ToastRootContext, isInCommitPhase, runInCommitPhase } from "./ToastRootContext.js";
 import { transitionStatusMapping } from "../../utils/stateAttributesMapping.js";
 import { useToastContext } from "../provider/ToastProviderContext.js";
 import { useRenderElement } from "../../utils/useRenderElement.js";
@@ -155,16 +155,28 @@ export const ToastRoot = /*#__PURE__*/React.forwardRef(function ToastRoot(compon
     element.style.height = 'auto';
     const height = element.offsetHeight;
     element.style.height = previousHeight;
-    ReactDOM.flushSync(() => {
+    // SPARX PATCH: flush only when we are NOT already inside a layout effect.
+    // React refuses to flush during the commit phase and warns instead, and the
+    // flush is redundant there anyway — a setState in a layout effect already
+    // re-renders synchronously before paint. See ToastRootContext.js.
+    const commitHeight = () => {
       setToasts(prev => prev.map(t => t.id === toast.id ? {
         ...t,
         ref: rootRef,
         height,
         transitionStatus: undefined
       } : t));
-    });
+    };
+    if (isInCommitPhase()) {
+      commitHeight();
+    } else {
+      ReactDOM.flushSync(commitHeight);
+    }
   });
-  useIsoLayoutEffect(recalculateHeight, [recalculateHeight]);
+  // SPARX PATCH: mark the layout-effect path so the flush above is skipped.
+  useIsoLayoutEffect(() => {
+    runInCommitPhase(recalculateHeight);
+  }, [recalculateHeight]);
   function applyDirectionalDamping(deltaX, deltaY) {
     let newDeltaX = deltaX;
     let newDeltaY = deltaY;
diff --git a/esm/toast/root/ToastRootContext.js b/esm/toast/root/ToastRootContext.js
index 8a6d883531df4c8a100f92729a7e0dd00c40351d..41a0e4227d4b9c0c5b4a436dd4d0e86de3017253 100644
--- a/esm/toast/root/ToastRootContext.js
+++ b/esm/toast/root/ToastRootContext.js
@@ -2,6 +2,45 @@ import _formatErrorMessage from "@base-ui-components/utils/formatErrorMessage";
 import * as React from 'react';
 export const ToastRootContext = /*#__PURE__*/React.createContext(undefined);
 if (process.env.NODE_ENV !== "production") ToastRootContext.displayName = "ToastRootContext";
+// ── SPARX PATCH ──────────────────────────────────────────────────────────────
+// `ToastRoot`'s `recalculateHeight` wraps its state update in
+// `ReactDOM.flushSync`, and two of its call sites are layout effects — its own,
+// and `ToastContent`'s. React is already inside the commit phase there, so it
+// refuses to flush and logs "flushSync was called from inside a lifecycle
+// method" for EVERY toast, every render. On a surface that fires a few toasts
+// that is a console full of noise the real errors then hide in.
+//
+// The flush is redundant on those two paths: a `setState` called during a
+// layout effect is already re-rendered synchronously before the browser paints,
+// which is the only thing the flush was buying. It is NOT redundant on the
+// ResizeObserver / MutationObserver paths, which run asynchronously after paint
+// and would otherwise let the stack visibly jump — those keep `flushSync`.
+//
+// So the two layout-effect call sites announce themselves through this counter
+// and `recalculateHeight` skips the flush while it is set. It lives here rather
+// than in ToastRoot because both files already import this module, so nothing
+// gains a new dependency edge.
+//
+// Remove when the upstream fix lands: 1.0.0-rc.0 is the newest published
+// version as of 2026-08-13.
+let commitPhaseDepth = 0;
+
+/** Run `fn` marked as being inside React's commit phase. */
+export function runInCommitPhase(fn) {
+  commitPhaseDepth += 1;
+  try {
+    return fn();
+  } finally {
+    commitPhaseDepth -= 1;
+  }
+}
+
+/** True while a layout effect is running `recalculateHeight`. */
+export function isInCommitPhase() {
+  return commitPhaseDepth > 0;
+}
+// ── END SPARX PATCH ──────────────────────────────────────────────────────────
+
 export function useToastRootContext() {
   const context = React.useContext(ToastRootContext);
   if (!context) {
diff --git a/toast/content/ToastContent.js b/toast/content/ToastContent.js
index f23fa58b46fb1eab667d099f0af98066edd5e5a7..e5f42ceebd535de92f0067515414b7daeba36762 100644
--- a/toast/content/ToastContent.js
+++ b/toast/content/ToastContent.js
@@ -33,7 +33,10 @@ const ToastContent = exports.ToastContent = /*#__PURE__*/React.forwardRef(functi
     if (!node) {
       return undefined;
     }
-    recalculateHeight();
+    // SPARX PATCH: this call is inside a layout effect, so mark it — see
+    // ToastRootContext.js. The two observers below are NOT: they fire
+    // asynchronously after paint and keep their synchronous flush.
+    (0, _ToastRootContext.runInCommitPhase)(recalculateHeight);
     if (typeof ResizeObserver !== 'function' || typeof MutationObserver !== 'function') {
       return undefined;
     }
diff --git a/toast/root/ToastRoot.js b/toast/root/ToastRoot.js
index 5d887553ccab90f6d633e9b39d6128c9b2e521a9..c66b1be668cdddaaba962cd0d674f66c9ededbed 100644
--- a/toast/root/ToastRoot.js
+++ b/toast/root/ToastRoot.js
@@ -161,16 +161,28 @@ const ToastRoot = exports.ToastRoot = /*#__PURE__*/React.forwardRef(function Toa
     element.style.height = 'auto';
     const height = element.offsetHeight;
     element.style.height = previousHeight;
-    ReactDOM.flushSync(() => {
+    // SPARX PATCH: flush only when we are NOT already inside a layout effect.
+    // React refuses to flush during the commit phase and warns instead, and the
+    // flush is redundant there anyway — a setState in a layout effect already
+    // re-renders synchronously before paint. See ToastRootContext.js.
+    const commitHeight = () => {
       setToasts(prev => prev.map(t => t.id === toast.id ? {
         ...t,
         ref: rootRef,
         height,
         transitionStatus: undefined
       } : t));
-    });
+    };
+    if ((0, _ToastRootContext.isInCommitPhase)()) {
+      commitHeight();
+    } else {
+      ReactDOM.flushSync(commitHeight);
+    }
   });
-  (0, _useIsoLayoutEffect.useIsoLayoutEffect)(recalculateHeight, [recalculateHeight]);
+  // SPARX PATCH: mark the layout-effect path so the flush above is skipped.
+  (0, _useIsoLayoutEffect.useIsoLayoutEffect)(() => {
+    (0, _ToastRootContext.runInCommitPhase)(recalculateHeight);
+  }, [recalculateHeight]);
   function applyDirectionalDamping(deltaX, deltaY) {
     let newDeltaX = deltaX;
     let newDeltaY = deltaY;
diff --git a/toast/root/ToastRootContext.js b/toast/root/ToastRootContext.js
index 33e757646bc640c92f532d5fc8f2fcff181e6386..bb45c758e8a471cf57ed5f6c5f30549431f90b5f 100644
--- a/toast/root/ToastRootContext.js
+++ b/toast/root/ToastRootContext.js
@@ -7,10 +7,36 @@ Object.defineProperty(exports, "__esModule", {
 });
 exports.ToastRootContext = void 0;
 exports.useToastRootContext = useToastRootContext;
+exports.runInCommitPhase = runInCommitPhase;
+exports.isInCommitPhase = isInCommitPhase;
 var _formatErrorMessage2 = _interopRequireDefault(require("@base-ui-components/utils/formatErrorMessage"));
 var React = _interopRequireWildcard(require("react"));
 const ToastRootContext = exports.ToastRootContext = /*#__PURE__*/React.createContext(undefined);
 if (process.env.NODE_ENV !== "production") ToastRootContext.displayName = "ToastRootContext";
+// ── SPARX PATCH ──────────────────────────────────────────────────────────────
+// See the ESM copy of this file for the full reasoning. In short: two of
+// `recalculateHeight`'s call sites are layout effects, React is already in the
+// commit phase there, and `ReactDOM.flushSync` therefore refuses to flush and
+// warns on every toast. The flush is redundant on those two paths and load
+// bearing on the observer paths, so the layout effects mark themselves here.
+let commitPhaseDepth = 0;
+
+/** Run `fn` marked as being inside React's commit phase. */
+function runInCommitPhase(fn) {
+  commitPhaseDepth += 1;
+  try {
+    return fn();
+  } finally {
+    commitPhaseDepth -= 1;
+  }
+}
+
+/** True while a layout effect is running `recalculateHeight`. */
+function isInCommitPhase() {
+  return commitPhaseDepth > 0;
+}
+// ── END SPARX PATCH ──────────────────────────────────────────────────────────
+
 function useToastRootContext() {
   const context = React.useContext(ToastRootContext);
   if (!context) {
