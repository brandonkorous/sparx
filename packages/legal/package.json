{
  "name": "@sparx/legal",
  "version": "0.0.0",
  "private": true,
  "description": "sparx's OWN platform legal document versions (docs/42 §6) — the single source of truth for ToS/Privacy/DPA/AUP versions, shared by the marketing pages that render them, the signUpMerchant acceptance recorder, and the re-acceptance check. Dependency-free.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
