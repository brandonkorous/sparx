{
  "name": "@sparx/db",
  "version": "0.0.0",
  "private": true,
  "description": "sparx data layer — Prisma client, schema, migrations, and tenant-context helpers for Cloud SQL Postgres 18.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./client": {
      "types": "./src/client.ts",
      "default": "./src/client.ts"
    }
  },
  "files": [
    "src",
    "prisma"
  ],
  "scripts": {
    "build": "prisma generate",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:reset": "prisma migrate reset --force",
    "db:studio": "prisma studio",
    "db:seed": "tsx prisma/seed.ts",
    "db:seed:platform": "tsx prisma/seed-platform.ts",
    "db:up": "docker compose -f docker-compose.yml up -d",
    "db:down": "docker compose -f docker-compose.yml down",
    "db:rls-audit": "tsx scripts/rls-audit.ts",
    "db:backfill:builder-class": "tsx scripts/backfill-builder-class.ts",
    "db:backfill:property-name": "tsx scripts/backfill-property-name.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0",
    "better-auth": "^1.6.11",
    "@google-cloud/secret-manager": "^5.6.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@sparx/blueprints": "workspace:*",
    "@sparx/builder-schemas": "workspace:*",
    "@sparx/commerce-schemas": "workspace:*",
    "@sparx/legal-templates": "workspace:*",
    "@sparx/marketplace-schemas": "workspace:*",
    "@sparx/silica-catalog": "workspace:*",
    "@types/node": "^25.9.1",
    "@types/pg": "^8.11.10",
    "prisma": "^6.2.0",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3"
  }
}
