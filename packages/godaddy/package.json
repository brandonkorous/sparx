{
  "name": "@sparx/godaddy",
  "version": "0.0.0",
  "private": true,
  "description": "GoDaddy Reseller API client — domain availability, purchase, DNS configuration, and lifecycle management. Used by api-rest (purchase flow), api-mcp (domain tools), and domain-worker.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
