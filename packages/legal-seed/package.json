{
  "name": "@sparx/legal-seed",
  "version": "0.0.0",
  "private": true,
  "description": "Seed a new tenant's starter legal pages + footer placements (docs/42 §3) into its CMS as editable drafts. Idempotent find-or-create over @sparx/legal-templates. Shared so BOTH the legal-seed-worker (prod, Pub/Sub) and the signup flow (dev/in-process) seed identically — single source of truth.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@sparx/db": "workspace:*",
    "@sparx/legal-templates": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
