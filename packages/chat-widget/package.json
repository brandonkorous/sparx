{
  "name": "@sparx/chat-widget",
  "version": "0.0.0",
  "private": true,
  "description": "sparx Live Chat storefront widget (docs/56, docs/69 A-4) — a self-contained floating chat bubble + panel that talks to api-rest's public chat surface over REST + the /ws/chat socket. Embeddable on any tenant storefront and on sparx.market.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1"
  },
  "peerDependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.0.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
