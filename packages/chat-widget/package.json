{
  "name": "@sparx/chat-widget",
  "version": "0.0.0",
  "private": true,
  "description": "sparx Live Chat storefront widget (docs/56, docs/69 A-4) — a floating chat bubble + panel, built on silicaui, that talks to api-rest's public chat surface over REST + the /ws/chat socket. Mounted inside sparx's own Next.js apps (site, market, web) only — never a standalone third-party embed.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@wizeworks/silicaui-react": "catalog:",
    "socket.io-client": "^4.8.1"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
