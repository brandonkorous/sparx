{
  "name": "@sparx/modules",
  "version": "0.0.0",
  "private": true,
  "description": "Module-enablement primitives (the 'disabled module = zero overhead' gate, locked decision #6). Reads tenants.settings.modules with a small in-process TTL cache. Session-free + dependency-light (only @sparx/db) so any backend — including lean workers — can probe module flags without pulling the auth/email/UI closure.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@sparx/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
