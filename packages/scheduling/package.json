{
  "name": "@sparx/scheduling",
  "version": "0.0.0",
  "private": true,
  "description": "sparx Scheduling engine (docs/79) — availability/slot computation, the booking lifecycle with a DB-level no-overlap guarantee, and resource/service/availability setup. One model, every booking shape (appointment / class / reservation / rental).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@sparx/db": "workspace:*",
    "@sparx/scheduling-schemas": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
