{
  "name": "@sparx/forms",
  "version": "0.0.0",
  "private": true,
  "description": "Client form-validation engine for sparx apps — drives silica's Field / FieldStatus treatment (control accent, status icon, message panel) with a uniform touched/submit/server-error state machine and composable rule builders. Consumed by dashboard, admin, and web.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "react": "^19.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
