{
  "name": "@sparx/blueprints",
  "version": "0.0.0",
  "private": true,
  "description": "sparx Tenant Blueprints (docs/54) — the declarative manifest format for marketplace templates that provision a whole tenant (brand/theme + content + commerce + pages + emails + components), plus the in-repo blueprint catalog and an integrity validator. Zod-only (no DB, no React) so the installer worker, api-rest, and the dashboard can all import it.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@sparx/builder-schemas": "workspace:*",
    "@sparx/cms-schemas": "workspace:*",
    "@sparx/commerce-schemas": "workspace:*",
    "@sparx/sitebuilder-schemas": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
