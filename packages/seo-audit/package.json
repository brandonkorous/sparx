{
  "name": "@sparx/seo-audit",
  "version": "0.0.0",
  "private": true,
  "description": "Dependency-free SEO scoring engine (docs/50 §7). auditEntity() grades a normalized AuditableEntity 0–100 across 12 checks; pure, deterministic, no I/O. Shared by api-rest's /v1/seo/audit and any surface that renders a scorecard.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
