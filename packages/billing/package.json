{
  "name": "@sparx/billing",
  "version": "0.0.0",
  "private": true,
  "description": "Platform billing — WizeWorks charging tenants per active module via a single Stripe subscription (one item per module). Lazy + idempotent customer/subscription provisioning, module-item sync, trial lifecycle, and webhook reconciliation. Guarded so every operation is a no-op until STRIPE_SECRET_KEY + price IDs are configured (dev/test never need Stripe).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "provision-stripe": "tsx scripts/provision-stripe.ts",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@sparx/db": "workspace:*",
    "@sparx/modules": "workspace:*",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
