{
  "name": "@sparx/commerce-indexer",
  "version": "0.0.0",
  "private": true,
  "description": "sparx search indexer. Cloud Run Pub/Sub push worker. Consumes catalog events (product.*, variant.*, inventory.adjusted) and — once the customer/order bridges land — crm.customer.* / order.* events, reprojecting the affected entity into Typesense via @sparx/commerce's projections + @sparx/search. Also runs admin-triggered full reindexes (search.reindex.requested).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch --clear-screen=false src/index.ts",
    "start": "node --import tsx src/index.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "@sparx/commerce": "workspace:*",
    "@sparx/db": "workspace:*",
    "@sparx/search": "workspace:*",
    "pino": "^9.5.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "dotenv": "^16.4.7",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
